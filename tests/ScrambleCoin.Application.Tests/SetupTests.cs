namespace ScrambleCoin.Application.Tests;

/// <summary>
/// Placeholder test — verifies the Application test project is correctly configured.
/// MediatR handler tests with NSubstitute mocks live here.
/// </summary>
public class SetupTests
{
    [Fact]
    public void Setup_IsValid()
    {
        // This test exists to confirm the Application.Tests project is correctly wired up.
        // Replace with real handler tests as commands/queries are introduced.
        Assert.True(true);
    }
}
