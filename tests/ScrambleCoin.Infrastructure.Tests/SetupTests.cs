namespace ScrambleCoin.Infrastructure.Tests;

/// <summary>
/// Placeholder test — verifies the Infrastructure test project is correctly configured.
/// EF Core repository tests against Docker SQL Server live here.
/// </summary>
public class SetupTests
{
    [Fact]
    public void Setup_IsValid()
    {
        // This test exists to confirm the Infrastructure.Tests project is correctly wired up.
        // Replace with real repository tests as EF Core entities are introduced.
        Assert.True(true);
    }
}
