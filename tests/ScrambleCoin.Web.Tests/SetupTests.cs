namespace ScrambleCoin.Web.Tests;

/// <summary>
/// Placeholder test — verifies the Web test project is correctly configured.
/// bUnit component tests and WebApplicationFactory API tests live here.
/// </summary>
public class SetupTests
{
    [Fact]
    public void Setup_IsValid()
    {
        // This test exists to confirm the Web.Tests project is correctly wired up.
        // Replace with real bUnit and WebApplicationFactory tests as components/endpoints are introduced.
        Assert.True(true);
    }
}
