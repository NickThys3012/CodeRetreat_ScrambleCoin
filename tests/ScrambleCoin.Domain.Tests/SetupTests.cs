namespace ScrambleCoin.Domain.Tests;

/// <summary>
/// Placeholder test — verifies the Domain test project is correctly configured.
/// Pure C# entity/rules tests live here (no mocks, no infrastructure).
/// </summary>
public class SetupTests
{
    [Fact]
    public void Setup_IsValid()
    {
        // This test exists to confirm the Domain.Tests project is correctly wired up.
        // Replace with real entity/rule tests as domain classes are introduced.
        Assert.True(true);
    }
}
